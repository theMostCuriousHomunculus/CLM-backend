type BlogPostType implements Document {
  _id: ID
  author: AccountType
  body: String
  comments: [MessageType]
  createdAt: String
  image: String
  subtitle: String
  title: String
  updatedAt: String
}
