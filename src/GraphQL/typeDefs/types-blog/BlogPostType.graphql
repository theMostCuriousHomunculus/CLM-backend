type BlogPostType implements Document {
  _id: ID
  author: AccountType
  body: String
  comments: [MessageType]
  createdAt: String
  image: String
  published: Boolean
  subtitle: String
  title: String
  updatedAt: String
}
