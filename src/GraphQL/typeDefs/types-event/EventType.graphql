type EventType implements Document {
  _id: ID
  chat_log: [MessageType]
  createdAt: String
  cube: CubeType
  finished: Boolean
  host: AccountType
  name: String
  players: [EventPlayerType]
  updatedAt: String
}
