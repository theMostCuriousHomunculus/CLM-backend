type DeckType implements Document {
  _id: ID!
  cards: [DeckCardType]!
  creator: AccountType!
  description: String
  format: FormatEnum
  image: ScryfallCardType
  name: String!
  published: Boolean
}
