type DeckType implements Document {
  _id: ID
  creator: AccountType
  description: String
  format: FormatEnum
  image: ScryfallCardType
  mainboard: [DeckCardType]
  name: String
  published: Boolean
  sideboard: [DeckCardType]
}
