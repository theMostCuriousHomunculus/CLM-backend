type CubeType implements Document {
  _id: ID
  creator: AccountType
  description: String
  image: String
  mainboard: [CubeCardType]
  modules: [ModuleType]
  name: String
  published: Boolean
  rotations: [RotationType]
  sideboard: [CubeCardType]
}
