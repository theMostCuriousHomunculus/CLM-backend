type CubeType implements Document {
  _id: ID
  creator: AccountType
  description: String
  image: ScryfallCardType
  mainboard: [CubeCardType]
  modules: [ModuleType]
  name: String
  published: Boolean
  rotations: [RotationType]
  sideboard: [CubeCardType]
}
